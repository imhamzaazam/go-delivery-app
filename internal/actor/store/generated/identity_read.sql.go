// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.25.0
// source: identity_read.sql

package actorstore

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const getActorProfileByMerchantAndEmail = `-- name: GetActorProfileByMerchantAndEmail :one
SELECT
    a.merchant_id,
    a.id AS uid,
    a.email,
    trim(concat(a.first_name, ' ', a.last_name)) AS full_name,
    a.is_active,
    a.last_login,
    a.created_at,
    a.modified_at
FROM actors a
WHERE a.merchant_id = $1
  AND a.email = $2
LIMIT 1
`

type GetActorProfileByMerchantAndEmailParams struct {
	MerchantID uuid.UUID
	Email      string
}

type GetActorProfileByMerchantAndEmailRow struct {
	MerchantID uuid.UUID
	UID        uuid.UUID
	Email      string
	FullName   string
	IsActive   bool
	LastLogin  time.Time
	CreatedAt  time.Time
	ModifiedAt time.Time
}

func (q *Queries) GetActorProfileByMerchantAndEmail(ctx context.Context, arg GetActorProfileByMerchantAndEmailParams) (GetActorProfileByMerchantAndEmailRow, error) {
	row := q.db.QueryRow(ctx, getActorProfileByMerchantAndEmail, arg.MerchantID, arg.Email)
	var i GetActorProfileByMerchantAndEmailRow
	err := row.Scan(
		&i.MerchantID,
		&i.UID,
		&i.Email,
		&i.FullName,
		&i.IsActive,
		&i.LastLogin,
		&i.CreatedAt,
		&i.ModifiedAt,
	)
	return i, err
}

const listActorsByMerchant = `-- name: ListActorsByMerchant :many
SELECT
    a.merchant_id,
    a.id AS uid,
    a.email,
    trim(concat(a.first_name, ' ', a.last_name)) AS full_name,
    a.is_active,
    a.last_login,
    a.created_at,
    a.modified_at
FROM actors a
WHERE a.merchant_id = $1
ORDER BY a.created_at DESC, a.id
`

type ListActorsByMerchantRow struct {
	MerchantID uuid.UUID
	UID        uuid.UUID
	Email      string
	FullName   string
	IsActive   bool
	LastLogin  time.Time
	CreatedAt  time.Time
	ModifiedAt time.Time
}

func (q *Queries) ListActorsByMerchant(ctx context.Context, merchantID uuid.UUID) ([]ListActorsByMerchantRow, error) {
	rows, err := q.db.Query(ctx, listActorsByMerchant, merchantID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListActorsByMerchantRow{}
	for rows.Next() {
		var i ListActorsByMerchantRow
		if err := rows.Scan(
			&i.MerchantID,
			&i.UID,
			&i.Email,
			&i.FullName,
			&i.IsActive,
			&i.LastLogin,
			&i.CreatedAt,
			&i.ModifiedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listEmployeesByMerchant = `-- name: ListEmployeesByMerchant :many
SELECT DISTINCT
    a.merchant_id,
    a.id AS uid,
    a.email,
    trim(concat(a.first_name, ' ', a.last_name)) AS full_name,
    a.is_active,
    a.last_login,
    a.created_at,
    a.modified_at
FROM actors a
JOIN actor_roles ar
  ON ar.merchant_id = a.merchant_id
 AND ar.actor_id = a.id
JOIN roles r
  ON r.merchant_id = ar.merchant_id
 AND r.id = ar.role_id
WHERE a.merchant_id = $1
  AND r.role_type = 'employee'
ORDER BY a.created_at DESC, a.id
`

type ListEmployeesByMerchantRow struct {
	MerchantID uuid.UUID
	UID        uuid.UUID
	Email      string
	FullName   string
	IsActive   bool
	LastLogin  time.Time
	CreatedAt  time.Time
	ModifiedAt time.Time
}

func (q *Queries) ListEmployeesByMerchant(ctx context.Context, merchantID uuid.UUID) ([]ListEmployeesByMerchantRow, error) {
	rows, err := q.db.Query(ctx, listEmployeesByMerchant, merchantID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListEmployeesByMerchantRow{}
	for rows.Next() {
		var i ListEmployeesByMerchantRow
		if err := rows.Scan(
			&i.MerchantID,
			&i.UID,
			&i.Email,
			&i.FullName,
			&i.IsActive,
			&i.LastLogin,
			&i.CreatedAt,
			&i.ModifiedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
